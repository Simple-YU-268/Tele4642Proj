# 酒店WiFi网络控制器 - 技术架构文档

## 📋 项目概述

本项目是一个基于SDN的酒店WiFi网络管理系统，采用Ryu控制器实现基于流量配额的动态访问控制。系统通过OpenFlow协议精确管理网络流量，为酒店客人提供房间号+手机号认证、套餐选择、设备绑定和流量配额管理功能。

## 🏗️ 系统架构设计

### **1. 三层架构模型**

```
┌─────────────────────────────────────────────────────────────┐
│                    系统架构图                                │
├─────────────────────────────────────────────────────────────┤
│  表示层         │  业务逻辑层     │  数据访问层   │ 网络层   │
│  ┌─────────────┐│  ┌─────────────┐│  ┌───────────┐│ ┌─────┐ │
│  │Web界面      ││  │配额管理     ││  │JSON文件   ││ │Open │ │
│  │REST API     ││  │流量统计     ││  │持久化     ││ │Flow │ │
│  └─────────────┘│  └─────────────┘│  └───────────┘│ └─────┘ │
└─────────────────────────────────────────────────────────────┘
```

### **2. 核心模块职责**

| 模块 | 职责 | 关键技术 |
|---|---|---|
| **FlowManager** | 流表管理 | OpenFlow协议、优先级匹配 |
| **QuotaManager** | 配额管理 | JSON持久化、实时更新 |
| **TrafficMonitor** | 流量统计 | 字节级精度、并发安全 |
| **APIController** | REST接口 | Flask、JSON响应 |

## 🔧 流表设计详解

### **流表优先级体系**

```
优先级层次（从高到低）：
├── 优先级400: 设备-路由器IP许可（配额控制）
├── 优先级1:   ARP通用许可（网络发现）
└── 优先级0:   table-miss drop（默认丢弃）
```

### **流表匹配规则**

#### **基础流表（静态）**
```python
# 优先级0: 丢弃所有未匹配流量
match = OFPMatch()
actions = []
priority = 0

# 优先级1: 允许所有ARP流量
match = OFPMatch(eth_type=0x0806)
actions = [OFPActionOutput(OFPP_FLOOD)]
priority = 1
```

#### **配额流表（动态）**
```python
# 优先级400: 设备到路由器的IP流量
match = OFPMatch(
    eth_src=device_mac,
    eth_dst=router_mac,
    eth_type=0x0800  # IPv4（包含ICMP、TCP、UDP）
)
actions = [OFPActionOutput(router_port)]
priority = 400
```

### **流表生命周期**

1. **初始化**: 交换机连接时安装基础流表
2. **配额下发**: 根据用户配额动态添加优先级400规则
3. **配额耗尽**: 自动删除优先级400规则，保留基础流表
4. **配额恢复**: 重新添加优先级400规则

## 📊 流量统计机制

### **统计精度与实时性**

| 指标 | 实现细节 |
|---|---|
| **统计单位** | 字节（Byte） |
| **实时性** | 数据包到达时立即统计 |
| **并发安全** | 线程锁保护 |
| **持久化** | JSON文件存储 |
| **精度验证** | 与系统实际流量对比 |

### **数据流向图**
```
数据包捕获 → 字节计数 → 配额扣除 → 流表更新
    ↓         ↓         ↓         ↓
PacketIn → TrafficMonitor → QuotaManager → FlowManager
```

### **统计数据结构**

#### **用户配额数据** (`user_data.json`)
```json
{
  "users": {
    "101": {
      "quota": 10737418240,        // 10GB (字节)
      "devices": ["00:00:00:00:00:01"],
      "used_traffic": 0,           // 已使用流量
      "created_at": 1752853806     // 创建时间戳
    }
  }
}
```

#### **流量统计数据** (`traffic_stats.json`)
```json
{
  "total": {
    "00:00:00:00:00:01": 1048576   // 累计流量
  },
  "daily": {
    "00:00:00:00:00:01": 524288    // 当日流量
  },
  "lastUpdate": 1731321600.0       // 最后更新时间
}
```

## 🔐 认证与授权流程

### **用户认证流程图**
```
用户连接WiFi → 访问认证页面 → 输入房间号+手机号 → 选择套餐 → 支付 → 设备绑定 → 开始上网
```

### **认证状态管理**
- **未认证**: 仅允许ARP流量
- **已认证有配额**: 允许ARP + IP流量
- **已认证无配额**: 仅允许ARP流量

### **设备绑定机制**
- **MAC地址绑定**: 每个房间可绑定多个设备
- **动态更新**: 设备MAC变化时实时更新流表
- **配额共享**: 同一房间所有设备共享配额

## 🌐 网络拓扑设计

### **Mininet拓扑结构**
```
   h1 (端口2)
     |
   h2 (端口3) --- s1 --- router (端口1)
     |
   h3 (端口4)
```

### **端口映射表**
| 设备 | MAC地址 | 交换机端口 | 用途 |
|---|---|---|---|
| h1 | 00:00:00:00:00:01 | 2 | 客房1 |
| h2 | 00:00:00:00:00:02 | 3 | 客房2 |
| h3 | 00:00:00:00:00:03 | 4 | 客房3 |
| router | 00:00:00:00:00:AA | 1 | 网关 |

## 📡 API接口设计

### **RESTful API规范**

#### **房间认证API (端口5000)**
| 端点 | 方法 | 描述 | 请求体 |
|---|---|---|---|
| `/room_login` | POST | 房间认证 | {room, phone_last4} |
| `/select_room_plan` | POST | 选择套餐 | {room, plan} |
| `/connect_room_device` | POST | 设备绑定 | {room, mac} |
| `/get_room_quota` | GET | 查询配额 | room参数 |

#### **SDN控制API (端口8080)**
| 端点 | 方法 | 描述 | 响应 |
|---|---|---|---|
| `/traffic` | GET | 流量统计 | JSON格式 |
| `/quota_status` | GET | 配额状态 | 设备列表 |
| `/add_quota` | POST | 增加配额 | {mac, bytes} |

## ⚡ 性能优化

### **流表优化策略**
1. **最小匹配**: 使用最精确的匹配字段
2. **优先级分层**: 避免规则冲突
3. **动态清理**: 及时删除过期规则
4. **批量操作**: 减少控制器与交换机交互

### **内存管理**
- **对象池**: 重用流表对象
- **缓存策略**: 热点数据内存缓存
- **垃圾回收**: 及时清理过期数据

### **并发处理**
- **线程安全**: 使用Lock保护共享数据
- **异步处理**: 非阻塞API响应
- **连接池**: 复用网络连接

## 🔍 调试与监控

### **调试工具**
```bash
# 查看流表状态
ovs-ofctl dump-flows s1

# 检查控制器日志
tail -f /tmp/ryu.log

# 验证网络连通性
sudo mn --test pingall

# 查看配额状态
curl http://localhost:8080/quota_status
```

### **监控指标**
- **流表数量**: 动态规则计数
- **流量统计**: 实时字节计数
- **错误率**: 认证失败统计
- **响应时间**: API延迟监控

## 🚀 扩展设计

### **水平扩展**
- **多控制器**: 支持控制器集群
- **负载均衡**: 流量分发到多个控制器
- **数据库**: 从JSON升级到MySQL/Redis

### **功能扩展**
- **QoS控制**: 基于优先级的带宽分配
- **内容过滤**: URL黑白名单
- **时间控制**: 基于时段的访问控制
- **多租户**: 支持多个酒店独立管理

## 📋 部署指南

### **开发环境**
```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动控制器
ryu-manager hotel_wifi_controller.py

# 3. 启动认证服务
python flask_room_auth.py

# 4. 启动网络
sudo python mininettopo.py
```

### **生产环境**
```bash
# 1. 系统配置
sudo systemctl enable ryu-controller
sudo systemctl enable flask-auth

# 2. 网络配置
# 配置物理交换机OpenFlow连接
# 设置VLAN隔离

# 3. 监控配置
# 部署Prometheus + Grafana
# 配置告警规则
```

## 🎯 故障排除

### **常见问题诊断**

#### **流表问题**
```bash
# 检查流表是否正确安装
ovs-ofctl dump-flows s1 | grep "priority=400"

# 验证MAC地址绑定
curl http://localhost:8080/quota_status
```

#### **流量统计问题**
```bash
# 检查流量文件
cat traffic_stats.json | jq .

# 验证配额扣除
cat user_data.json | jq '.users[].used_traffic'
```

#### **网络连接问题**
```bash
# 检查OpenFlow连接
ovs-vsctl show

# 测试主机连通性
h1 ping -c 3 10.0.0.100
```

## 📊 测试用例

### **功能测试**
1. **认证测试**: 房间号+手机号认证
2. **配额测试**: 流量使用与配额扣除
3. **设备测试**: MAC地址绑定与解绑
4. **套餐测试**: 不同套餐的流量限制

### **性能测试**
1. **并发测试**: 1000设备同时在线
2. **流量测试**: 大流量下的稳定性
3. **恢复测试**: 配额耗尽后的恢复

## 🔐 安全考虑

### **数据安全**
- **加密传输**: HTTPS API接口
- **数据脱敏**: 手机号部分隐藏
- **访问控制**: 基于角色的权限管理

### **网络安全**
- **MAC欺骗防护**: 绑定验证
- **ARP防护**: 静态ARP表
- **DoS防护**: 流量限速

---

**技术文档版本**: v2.0  
**最后更新**: 2025-07-24  
**维护者**: Simple-YU-268
